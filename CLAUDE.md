# SEO 專案操作手冊

網站：huitong1929.com（惠通行）
目標：用 Google Search Console 的真實資料，找出最划算的 SEO 改善項目，然後逐一執行。

---

## 這個專案在做什麼

Google Search Console（GSC）會記錄每個關鍵字帶來多少「曝光」和「點擊」。

- **曝光**：有人搜尋這個字、你的頁面出現在結果裡，但他不一定點進來
- **點擊**：他真的點進你的頁面
- **CTR**：點擊 ÷ 曝光，代表「看到你的人裡面有幾 % 點進來」
- **排名**：你平均出現在第幾名

SEO 能做的事分兩種：
1. **不用寫新文章**：改 title 和 meta description，讓看到你的人更願意點進來（提升 CTR）
2. **寫新文章**：讓更多頁面出現在搜尋結果裡（提升曝光）

這個專案的工作流程是：拉資料 → 分析機會 → 決定做哪件事 → 執行。

---

## 工具說明

### `/fetch`（拉資料）
```
python scripts/fetch_gsc_data.py --days 90
```
從 GSC API 拉最近 90 天資料，存成 JSON 放在 `data/` 裡。
每次拉的資料有 2-3 天延遲（GSC 本身的限制）。

### `/analyze`（找機會）
```
python scripts/analyze.py
```
讀取最新資料，自動把所有關鍵字分成三類，報告存在 `reports/` 裡。

### `/act`（執行）
讀取最新的 `reports/analysis_<日期>.md`，針對每個項目給出具體的修改建議或文章草稿。

**執行前的必要步驟（不能跳過）：**
1. 先用 `scripts/scrape_meta.py` 抓所有文章的現有 title 和 meta
2. 再逐一爬每篇文章的完整內文，確認實際涵蓋的內容
3. 對照 GSC 關鍵字和文章內文，才輸出修改建議
4. 建議文字必須根據文章實際說的內容寫——不能憑 title 推測、不能加入文章沒提到的東西

---

## 三類機會的判斷邏輯

### A 類：改 title/meta（最快見效）

**條件**：曝光 > 2,000 且 CTR < 3%

**意思**：這個關鍵字已經有大量的人看到你，但只有不到 3% 的人點進來。
問題幾乎都在 title 或 meta description 寫得不夠直接，搜尋者看不出你的頁面會回答他的問題。

**該怎麼改**：
- Title 要直接回答搜尋意圖（搜「電鍋煮飯水比例」的人，title 就要有「水比例」）
- Meta description 要給一個點進去的理由（具體數字、對比、解答）
- 改完通常 2-4 週後 CTR 會開始變化

**優先順序**：曝光越高越先做，因為受眾最大。

### B 類：強化現有文章（中期）

**條件**：曝光 > 500 且排名在 6-15 名

**意思**：這個關鍵字你已經排到第一頁邊緣或第二頁，再推一把就能進前五。
排名 6-15 的 CTR 比前五名低很多（第 1 名約 28%，第 6 名約 6%），值得投資。

**該怎麼做**：
- 補充文章內容（字數、資訊密度）
- 加入搜尋者真正在找的子問題（看關鍵字變體）
- 改善頁面結構（H2、H3 是否對應到搜尋意圖）

**優先順序**：曝光越高越先做。

### C 類：寫新文章（長期）

**條件**：曝光 > 800 且排名 > 15

**意思**：有人在搜這個字、你也有點相關度（Google 才會給你曝光），但你沒有一篇夠好的文章來承接這個流量。這是需要從零開始寫文章的機會。

**優先順序**：曝光越高越先寫。

---

## 執行流程

每次進入這個專案：

1. 先跑 `/fetch` 拿最新資料
2. 跑 `/analyze` 看分類結果
3. 從 A 類最高曝光的項目開始，逐一給出修改建議
4. 用戶確認後，整理成可以直接貼到 Shopify 的格式
5. 做完 A 類再處理 B 類

每隔 2-4 週重新 `/fetch` + `/analyze`，追蹤改善效果。

---

## 在 Shopify 哪裡改

網路商店 → 網誌 → 選文章 → 拉到最下面「搜尋引擎列表預覽」→ 點「編輯網站 SEO」
- **頁面標題**：對應 Google 搜尋結果的藍色標題
- **說明**：對應 Google 搜尋結果的灰色描述文字

---

## 判斷改善效果

改完 title/meta 後，等 4 週再 `/fetch` 比對：
- CTR 有沒有上升
- 點擊數有沒有增加（即使排名沒變）

如果 CTR 上升但點擊沒增加，代表排名掉了；如果兩個都上升，代表成功。
